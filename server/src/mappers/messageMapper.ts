// import { IMessage } from "../models/messageModel";
// import { ChatMessageResponseDTO } from "../dtos/message.dto";

// export const mapMessageToDTO = (message: IMessage): ChatMessageResponseDTO => ({
//   id: message._id.toString(),
//   senderId: message.sender.toString(),
//   senderModel: message.senderModel,
//   receiverId: message.receiver.toString(),
//   receiverModel: message.receiverModel,
//   propertyId: message.propertyId.toString(),
//   content: message.content,
//   image: message.image,
//   isRead: message.isRead,
//   createdAt: message.createdAt,
// });
